<% @project.tags.build %>
<%= form_for(@project) do |project_form| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= project_form.label :name %><br />
    <%= project_form.text_field :name %>
  </div>
  <div class="field">
    <%= project_form.label :description  %><br />
    <%= project_form.text_field :description %>
  </div>
  <div class="field">
    <%= project_form.label :start_date %><br />
    <%= project_form.date_select :start_date %>
  </div>
  <div class="field">
    <%= project_form.label :end_date %><br />
    <%= project_form.date_select :end_date %>
  </div>
  <h2>Users</h2>
  <%= render :partial => 'tags/form',
             :locals => {:form => project_form} %>
  <div class="actions">
    <%= project_form.submit %>
  </div>
<% end %>
